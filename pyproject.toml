[project]
name = "pytrade.org"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "afterpython>=0.3.11",
    "juv>=0.5.0",
]

[dependency-groups]
dev = [
    "marimo[lsp,recommended]>=0.23.8",
]

[tool.uv]
package = false
