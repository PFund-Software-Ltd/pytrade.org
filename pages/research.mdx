import { ReposTable } from '../components/ReposTable.mdx';
import PypiBadge from '../components/PypiBadge';
import getGitHubReposData from '../utils/getGitHubReposData';

export const REPOS = {
  openbb: {
    pypi: 'openbb',
    github: 'OpenBB-Finance/OpenBBTerminal',
    // description: '',  // optional
  },
  'scikit-learn': {
    pypi: 'scikit-learn',
    github: 'scikit-learn/scikit-learn',
    // description: '',  // optional
  },
  torch: {
    pypi: 'torch',
    github: 'pytorch/pytorch',
    // description: '',  // optional
  },
  'pyro-ppl': {
    pypi: 'pyro-ppl',
    github: 'pyro-ppl/pyro',
    // description: '',  // optional
  },
  qlib: {
    pypi: 'pyqlib',
    github: 'microsoft/qlib',
    // description: '',  // optional
  },
};

export const getStaticProps = async ({ params }) => {
  const githubRepos = Object.values(REPOS).map(repo => repo.github);
  return await getGitHubReposData(githubRepos);
};

## Research Tools
<ReposTable />


## openbb <PypiBadge pypiName={REPOS['openbb']['pypi']} />
{REPOS['openbb']?.description}


## scikit-learn <PypiBadge pypiName={REPOS['scikit-learn']['pypi']} />
{REPOS['scikit-learn']?.description}


## torch <PypiBadge pypiName={REPOS['torch']['pypi']} />
{REPOS['torch']?.description}


## pyro-ppl <PypiBadge pypiName={REPOS['pyro-ppl']['pypi']} />
{REPOS['pyro-ppl']?.description}


## qlib <PypiBadge pypiName={REPOS['qlib']['pypi']} />
{REPOS['qlib']?.description}
